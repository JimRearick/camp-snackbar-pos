# Camp Snackbar POS - Environment Configuration
# Copy this file to .env and customize the values

# Flask Secret Key (REQUIRED - change this!)
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=please-change-this-to-a-random-secret-key

# Flask Environment
FLASK_ENV=production

# Database path (inside container)
DB_PATH=/app/data/camp_snackbar.db

# Backup directory (inside container)
BACKUP_DIR=/app/backups

# Application Settings
# Uncomment and customize as needed

# CORS Origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000,https://yourdomain.com

# Session Configuration
# SESSION_COOKIE_SECURE=True  # Requires HTTPS
# SESSION_COOKIE_HTTPONLY=True
# SESSION_COOKIE_SAMESITE=Lax
# PERMANENT_SESSION_LIFETIME=3600  # 1 hour in seconds

# Logging
# LOG_LEVEL=INFO
# LOG_FILE=/app/logs/app.log

# Domain for Caddy automatic HTTPS (optional)
# DOMAIN=snackbar.example.com
